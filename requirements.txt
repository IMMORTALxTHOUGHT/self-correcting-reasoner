# Core ML
# Pinned for CUDA 12.4 / Python 3.10. TRL 0.20+ exposes the vLLM generation
# backend (vllm_device / vllm_gpu_memory_utilization), which grpo_trainer.py
# uses for a 3-5x GRPO speedup. transformers/peft are kept in sync.
trl==0.20.0
transformers==4.51.3
peft==0.15.2
bitsandbytes==0.45.5
accelerate==1.6.0

# Math verification
math-verify

# Inference (vLLM generation backend for GRPO). The +cu124 wheel matches
# CUDA 12.4. If your pip resolves a different CUDA tag, adjust accordingly.
vllm==0.8.5.post1+cu124

# Data
datasets==3.5.0
pandas
numpy

# Utilities
tqdm
wandb
tensorboard
